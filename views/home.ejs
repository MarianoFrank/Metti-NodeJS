<div class="hero">
  <h1>Encuentra o crea un grupo para compartir lo que más te gusta</h1>
  <a href="/crear-cuenta" class="btn btn-amarillo">Obtener Cuenta</a>
</div>

<div class="buscador-inicio">
  <div class="contenedor">
    <h2>Busca un grupo en tu ciudad</h2>
    <form action="/busqueda" method="GET" class="form-busqueda">
      <div class="campo-busqueda">
        <select name="categoria">
          <option selected value="">-- Todas --</option>
          <% categorias.forEach(categoria => { %>
          <option value="<%= categoria.id %>"><%= categoria.name %></option>
          <% }); %>
        </select>
        <input
          type="text"
          name="titulo"
          placeholder="Nombre Meeti (Opcional)"
        />
      </div>

      <div class="campo-busqueda">
        <input
          type="number"
          min="1"
          max="10"
          name="radio"
          placeholder="Radio en Km, ej: 20"
          class="radio"
        />
        <div class="campo-direccion">
          <input type="text" placeholder="Centro" id="direccionBusqueda" />
          <span class="loader"></span>
          <ul class="resultados-busqueda" style="display: none"></ul>
          <button class="btn btn-azul" id="btnUbiActual" type="button">Ubicacion actual</button>
          <input type="hidden" name="coordenadas" id="coordenadas" />
        </div>
      </div>

      <input type="submit" value="Buscar" class="btn btn-rosa" />
    </form>
    <div id="mapaBusqueda"></div>
  </div>
</div>

<% if(meetis.length > 0){ %>
<div class="contenedor">
  <h2>Próximos Meeti's</h2>
  <div class="grid columnas-md-3">
    <% meetis.forEach(meeti => { %> <%- include("./partials/card-meeti.ejs", {
    meeti }) %> <% }); %>
  </div>
</div>
<% } %>

<main class="bg-gris categorias">
  <h2>Encuentra Meeti's por Categoría</h2>

  <div class="contenedor">
    <ul class="listado-categorias grid columnas-2 columnas-md-3">
      <% categorias.forEach(categoria => { %>
      <li>
        <a href="/categoria/<%= categoria.name %>">
          <img src="<%- `/img/categoria_${categoria.id}.jpg` %>" />
          <p><%= categoria.name %></p>
        </a>
      </li>
      <% }); %>
    </ul>
  </div>
</main>

<div class="meeti-bg">
  <div class="contenedor meeti grid columnas-md-2">
    <section>
      <h3>Crea un grupo</h3>
      <p>
        Comparte lo que te gusta o tu pasión con los demás, crea tu propio grupo
        hoy mismo sin costos
      </p>
    </section>
    <section>
      <h3>Únete a un grupo</h3>
      <p>
        Conoce personas con tus mismos gustos y pasiones, únete a un grupo ya
        existente y recibe notificaciones sobre próximos eventos
      </p>
    </section>
  </div>
</div>
